# vim: sw=2 et sts=2

# need stage minify


minify:
  image: node:latest
  stage: min
  before_script:
    - npm install uglify-js -g
    - npm install uglifycss -g
  script:
  - ./minify.sh
  artifacts:
  only:
  - master


