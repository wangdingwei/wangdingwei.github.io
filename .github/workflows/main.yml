# vim: set sw=2 sts=2 et:     
# encoding: utf-8

name: purge-cache
on: [push]

jobs:
  build:
    name: purge
    runs-on: ubuntu-18.04
    steps:
    - name: purge
      run: |
        pwd
        ls -l
        ./purge-cache.sh

