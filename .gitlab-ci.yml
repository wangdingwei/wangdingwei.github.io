# vim: set sw=2 sts=2 et:     
# encoding: utf-8

image: alpine:latest

pages:
  stage: deploy
  script:
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master

purge-cache:
  stage: deploy
  script:
  - pwd
  - ls
  - ./purge-cache.sh
